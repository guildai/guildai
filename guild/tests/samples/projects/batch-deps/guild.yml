train:
  main: train
  flags:
    lr: [0.001,0.01,0.1]

serve:
  main: serve
  requires:
    - operation: train

  label: model=${train}

delme:
  main: guild.pass
  requires:
    - file: guild.yml
      flag-name: foo
      rename: guild.yml foo.yml
