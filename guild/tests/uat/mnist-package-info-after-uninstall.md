# `mnist` package info after uninstall

Once we've uninstalled `mnist`, we can no longer get its package info:

    >>> run("guild packages info gpkg.mnist")
    WARNING: unknown package gpkg.mnist
    <exit 0>
