- model: mnist
  operations:
    train:
      main: mnist
      flags:
        epochs:
          description: Number of epochs to train
          default: 5
        dropout:
          description: Dropout rate
          default: 0.2
        log_dir:
          default: .
